import 'package:flutter/material.dart';

class AuthRegisterWidget extends StatelessWidget {
  const AuthRegisterWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: Implement register widgets
    return Container();
  }
}
