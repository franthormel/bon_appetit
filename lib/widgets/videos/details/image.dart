import 'package:flutter/material.dart';

import '../../others/horizontal_padding.dart';

class VideoDetailsImageWidget extends StatelessWidget {
  const VideoDetailsImageWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const HorizontalPaddingWidget(
      child: Text(
        "TODO: Image hero with title and centered play button [imageUrl]",
      ),
    );
  }
}
