import 'package:flutter/material.dart';

class SearchPageFiltersWidget extends StatelessWidget {
  const SearchPageFiltersWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
