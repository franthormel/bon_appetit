import 'package:flutter/material.dart';

class SearchPageResultsHaveTextWidget extends StatelessWidget {
  const SearchPageResultsHaveTextWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Many Results");
  }
}
