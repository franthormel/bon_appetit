import 'package:flutter/material.dart';

class SearchPageSortByWidget extends StatelessWidget {
  const SearchPageSortByWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Sort by");
  }
}
