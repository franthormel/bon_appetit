import 'package:flutter/material.dart';

class RecipeDetailsNutritionWidget extends StatelessWidget {
  const RecipeDetailsNutritionWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('TODO nutrition');
  }
}
