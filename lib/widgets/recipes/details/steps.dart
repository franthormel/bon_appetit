import 'package:flutter/material.dart';

class RecipeDetailsStepsWidget extends StatelessWidget {
  const RecipeDetailsStepsWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('TODO steps');
  }
}
