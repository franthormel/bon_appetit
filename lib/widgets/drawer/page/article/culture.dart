import 'package:flutter/material.dart';

import '../../../../models/index.dart';

class DrawerPageArticleCultureWidget extends StatelessWidget {
  final Article article;

  const DrawerPageArticleCultureWidget(
    this.article, {
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
