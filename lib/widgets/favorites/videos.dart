import 'package:flutter/material.dart';

class FavoriteVideosWidget extends StatelessWidget {
  const FavoriteVideosWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: (FavoritesProvider) Put list of favorite videos here.
    return const Center(child: Text("TODO: Favorite videos"));
  }
}
