import 'package:flutter/material.dart';

class FavoriteArticlesWidget extends StatelessWidget {
  const FavoriteArticlesWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: (FavoritesProvider) Put list of favorite articles here.
    return const Center(child: Text("TODO: Favorite articles"));
  }
}
