import 'package:flutter/material.dart';

class FavoriteRecipesWidget extends StatelessWidget {
  const FavoriteRecipesWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: (FavoritesProvider) Put list of favorite recipes here.
    return const Center(child: Text("TODO: Favorite recipes"));
  }
}
